{
  "name": "kimjod-release-center",
  "version": "1.0.0",
  "private": true,
  "description": "Local one-click release and usage dashboard for Kimjod",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check lib/core.js && node --check web/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
